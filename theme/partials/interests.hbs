{{#if resume.interests.length}}
{{#if resume.meta.properties.theme.skyoverflow.interests.disabled}}{{else}}
<section class="section">
  <header>
    <h2 class='section-title' class='section-title'>
      {{#if resume.meta.properties.theme.skyoverflow.interests.title}}
        {{resume.meta.properties.theme.skyoverflow.interests.title}}
      {{else}}
        Interests
      {{/if}}
    </h2>
  </header>
  <section id="interests">
    {{#each resume.interests}}
    <div class="item">
      {{#if name}}
      <h3 class="name">
        {{name}}
      </h3>
      {{/if}}
      {{#if keywords.length}}
      <ul class="keywords">
        {{#each keywords}}
        <li>{{.}}</li>
        {{/each}}
      </ul>
      {{/if}}
      {{#if summary}}
        <div class="summary">{{paragraphSplit summary}}</div>
      {{/if}}
    </div>
    {{/each}}
  </section>
</section>
{{/if}}
{{/if}}