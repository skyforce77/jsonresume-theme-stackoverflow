{{#if resume.awards.length}}
{{#if resume.meta.properties.theme.skyoverflow.awards.disabled}}{{else}}
  <section class="section">
    <header>
      <h2 class="section-title">
        {{#if resume.meta.properties.theme.skyoverflow.awards.title}}
          {{resume.meta.properties.theme.skyoverflow.awards.title}}
        {{else}}
          Awards
        {{/if}}
      </h2>
    </header>
    <section id="awards">
      {{#each resume.awards}}
        <section class="award-item">
          {{#if summary}}
            <label for="award-item-{{@index}}"></label>
          {{/if}}

          <header class="clear">
            {{#if date}}
              <div class="date">
                {{Y date}}
              </div>
            {{/if}}
            <div class="header-left">
              {{#if title}}
                <div class="title">
                  {{title}}
                </div>
              {{/if}}
              {{#if awarder}}
                <div class="awarder">
                  {{awarder}}
                </div>
              {{/if}}
            </div>
          </header>

          <div class="item">
            {{#if summary}}
              <div class="summary">{{paragraphSplit summary}}</div>
            {{/if}}
          </div>
        </section>
      {{/each}}
    </section>
  </section>
{{/if}}
{{/if}}